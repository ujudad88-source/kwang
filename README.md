# 광전기통신 영업지원시스템

## 현재 버전
v0.0.9

## 배포
이 폴더의 전체 파일을 GitHub 저장소 루트에 업로드하면 GitHub Pages에서 정적 웹앱으로 사용할 수 있습니다.



## v0.0.9 변경사항
- 함 파트의 제품 선택 영역을 `함 제품`과 `함 관련 자재`로 분리했습니다.
- `PVC속판/단프라`를 함 제품 목록에서 제거하고 우측 `함 관련 자재` 선택창으로 이동했습니다.
- 두 선택창은 동시에 선택되지 않도록 연동했으며 기존 규격표와 단가 조정 기능을 그대로 사용합니다.
- 모바일에서는 선택창이 세로로 정렬되도록 반응형 처리했습니다.

- 함 파트 제품 목록에 `PVC속판/단프라`를 추가했습니다.
- 가로·세로별 PVC속판 및 단프라 단가 11개 규격을 등록했습니다.
- PVC속판/단프라 단가에도 기존 단가 조정 기능이 동일하게 적용됩니다.
- 깊이가 없는 판재 규격 선택 시 손도면 W/H/D 값이 잘못 덮어쓰이지 않도록 처리했습니다.
- 서비스 워커 캐시 버전을 v0.0.9로 갱신했습니다.

## v0.0.7 변경사항
- 메인 카드의 한 줄 요약을 간단하고 명료하게 축약했습니다.
- 단가 조정 계산기에서 모든 마이너스 빠른 선택 버튼을 삭제했습니다.
- 직접 입력 조정률의 최솟값을 0%로 제한했습니다.
- 이전 버전에서 저장된 음수 조정률은 자동으로 0%로 초기화됩니다.
- 서비스 워커 캐시 버전을 v0.0.7로 갱신했습니다.

## v0.0.6 변경사항
- 모바일 메인 카드를 `제목 + 한 줄 요약` 구조로 확정했습니다.
- iPhone Safari와 Android Chrome에서 설명이 한 글자씩 세로로 깨지는 현상을 방지했습니다.
- 모바일 카드 높이를 줄이고 카드 전체를 터치할 수 있도록 유지했습니다.
- 메인 카드의 큰 원형 장식을 제거하고 화살표를 간결하게 정리했습니다.
- 기존 v0.0.5의 로그인 액션, 화면 이력, iPhone 제스처 뒤로가기, Android 뒤로가기 기능을 유지했습니다.
- 변경 이력 파일을 `CHANGELOG.md` 대신 `README.md`로 통합했습니다.

## 관리 원칙
- GitHub Pages에서 바로 실행할 수 있도록 HTML, CSS, JavaScript 기반의 정적 구조를 유지합니다.
- 외부 빌드 도구 없이 수정·검증·배포가 가능하도록 관리합니다.
- 기존 기능은 유지하고 요청된 영역만 최소 범위로 변경합니다.

## 주요 파일
- `index.html`: 메인 애플리케이션
- `drawing_srcdoc_v82.html`: 손도면 관련 내장 화면
- `manifest.webmanifest`: PWA 설정
- `service-worker.js`: 오프라인 캐시
- `assets/`, `icons/`: 이미지와 앱 아이콘
